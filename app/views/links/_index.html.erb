
<ul>
  <% @links.each_with_index do |link, index| %>
    
      Link <%=index + 1 %>: <%= link_to "Where does this lead?", {:controller => "links", :action => "index", :name => link, remote: true } %>

  <%end%>
</ul>

